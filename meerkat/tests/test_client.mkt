import s1

service s2 {
    pub def inc_delegate = fn n => s1.inc_x;
    var w = s1.y - 2;
    pub def z = s1.y * 2;
}


@test(s2) {
    let cur_y = s1.y;
    do inc_delegate(1);
    assert (z == cur_y * 2 + 2);
    assert (w == cur_y - 2);
}
